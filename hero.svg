<svg width="900" height="480" viewBox="0 0 900 480" fill="none" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <!-- Background gradient -->
    <linearGradient id="bgGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#020c1b"/>
      <stop offset="50%" style="stop-color:#050d1a"/>
      <stop offset="100%" style="stop-color:#030a15"/>
    </linearGradient>

    <!-- Teal glow filter -->
    <filter id="tealGlow" x="-30%" y="-30%" width="160%" height="160%">
      <feGaussianBlur stdDeviation="4" result="blur"/>
      <feComposite in="SourceGraphic" in2="blur" operator="over"/>
    </filter>
    <filter id="tealGlowStrong" x="-50%" y="-50%" width="200%" height="200%">
      <feGaussianBlur stdDeviation="8" result="blur"/>
      <feComposite in="SourceGraphic" in2="blur" operator="over"/>
    </filter>
    <filter id="glowText" x="-10%" y="-10%" width="120%" height="120%">
      <feGaussianBlur stdDeviation="3" result="blur"/>
      <feMerge><feMergeNode in="blur"/><feMergeNode in="SourceGraphic"/></feMerge>
    </filter>
    <filter id="purpleGlow" x="-50%" y="-50%" width="200%" height="200%">
      <feGaussianBlur stdDeviation="6" result="blur"/>
      <feComposite in="SourceGraphic" in2="blur" operator="over"/>
    </filter>

    <!-- Radar gradient -->
    <radialGradient id="radarBg" cx="50%" cy="50%" r="50%">
      <stop offset="0%" style="stop-color:#0a2040;stop-opacity:0.9"/>
      <stop offset="100%" style="stop-color:#020c1b;stop-opacity:0.4"/>
    </radialGradient>

    <!-- Scan sweep gradient -->
    <linearGradient id="sweepGrad" x1="50%" y1="0%" x2="50%" y2="100%" gradientTransform="rotate(0, 0.5, 0.5)">
      <stop offset="0%" style="stop-color:#00f5d4;stop-opacity:0"/>
      <stop offset="80%" style="stop-color:#00f5d4;stop-opacity:0.15"/>
      <stop offset="100%" style="stop-color:#00f5d4;stop-opacity:0.5"/>
    </linearGradient>

    <!-- Progress bar gradient -->
    <linearGradient id="barExec" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" style="stop-color:#00f5d4"/>
      <stop offset="100%" style="stop-color:#00c9a7"/>
    </linearGradient>
    <linearGradient id="barSec" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" style="stop-color:#a855f7"/>
      <stop offset="100%" style="stop-color:#7c3aed"/>
    </linearGradient>
    <linearGradient id="barDel" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" style="stop-color:#f59e0b"/>
      <stop offset="100%" style="stop-color:#d97706"/>
    </linearGradient>

    <!-- Clip path for SVG border -->
    <clipPath id="mainClip">
      <rect x="1" y="1" width="898" height="478" rx="16"/>
    </clipPath>

    <!-- Shimmer gradient for bars -->
    <linearGradient id="shimmer" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" style="stop-color:rgba(255,255,255,0);stop-opacity:0"/>
      <stop offset="50%" style="stop-color:rgba(255,255,255,0.4);stop-opacity:1"/>
      <stop offset="100%" style="stop-color:rgba(255,255,255,0);stop-opacity:0"/>
    </linearGradient>
  </defs>

  <!-- ═══════════════ BACKGROUND ═══════════════ -->
  <rect width="900" height="480" rx="16" fill="url(#bgGrad)"/>

  <!-- Outer glow border -->
  <rect x="1" y="1" width="898" height="478" rx="16" fill="none" stroke="#00f5d4" stroke-width="1.5" stroke-opacity="0.5"/>
  <!-- Inner subtle border -->
  <rect x="3" y="3" width="894" height="474" rx="14" fill="none" stroke="#00f5d4" stroke-width="0.5" stroke-opacity="0.2"/>

  <!-- Corner accent marks -->
  <line x1="0" y1="40" x2="40" y2="0" stroke="#00f5d4" stroke-width="1" stroke-opacity="0.6" transform="translate(1,1)"/>
  <line x1="860" y1="1" x2="900" y2="41" stroke="#00f5d4" stroke-width="1" stroke-opacity="0.6"/>
  <line x1="1" y1="439" x2="41" y2="479" stroke="#00f5d4" stroke-width="1" stroke-opacity="0.6"/>
  <line x1="859" y1="479" x2="899" y2="439" stroke="#00f5d4" stroke-width="1" stroke-opacity="0.6"/>

  <!-- Grid background dots (static, layered) -->
  <g opacity="0.12" clip-path="url(#mainClip)">
    <!-- Vertical lines -->
    <line x1="75" y1="0" x2="75" y2="480" stroke="#00f5d4" stroke-width="0.5"/>
    <line x1="150" y1="0" x2="150" y2="480" stroke="#00f5d4" stroke-width="0.5"/>
    <line x1="225" y1="0" x2="225" y2="480" stroke="#00f5d4" stroke-width="0.5"/>
    <line x1="300" y1="0" x2="300" y2="480" stroke="#00f5d4" stroke-width="0.5"/>
    <line x1="375" y1="0" x2="375" y2="480" stroke="#00f5d4" stroke-width="0.5"/>
    <line x1="450" y1="0" x2="450" y2="480" stroke="#00f5d4" stroke-width="0.5"/>
    <line x1="525" y1="0" x2="525" y2="480" stroke="#00f5d4" stroke-width="0.5"/>
    <line x1="600" y1="0" x2="600" y2="480" stroke="#00f5d4" stroke-width="0.5"/>
    <line x1="675" y1="0" x2="675" y2="480" stroke="#00f5d4" stroke-width="0.5"/>
    <line x1="750" y1="0" x2="750" y2="480" stroke="#00f5d4" stroke-width="0.5"/>
    <line x1="825" y1="0" x2="825" y2="480" stroke="#00f5d4" stroke-width="0.5"/>
    <!-- Horizontal lines -->
    <line x1="0" y1="60" x2="900" y2="60" stroke="#00f5d4" stroke-width="0.5"/>
    <line x1="0" y1="120" x2="900" y2="120" stroke="#00f5d4" stroke-width="0.5"/>
    <line x1="0" y1="180" x2="900" y2="180" stroke="#00f5d4" stroke-width="0.5"/>
    <line x1="0" y1="240" x2="900" y2="240" stroke="#00f5d4" stroke-width="0.5"/>
    <line x1="0" y1="300" x2="900" y2="300" stroke="#00f5d4" stroke-width="0.5"/>
    <line x1="0" y1="360" x2="900" y2="360" stroke="#00f5d4" stroke-width="0.5"/>
    <line x1="0" y1="420" x2="900" y2="420" stroke="#00f5d4" stroke-width="0.5"/>
  </g>

  <!-- Ambient glow circles in background -->
  <circle cx="180" cy="240" r="200" fill="#00f5d4" opacity="0.025"/>
  <circle cx="680" cy="200" r="180" fill="#a855f7" opacity="0.03"/>

  <!-- ═══════════════ SCANLINE BOOT EFFECT ═══════════════ -->
  <foreignObject x="0" y="0" width="900" height="480">
    <div xmlns="http://www.w3.org/1999/xhtml">
      <style>
        @keyframes scanBoot {
          0% { top: -100%; opacity: 0.7; }
          100% { top: 120%; opacity: 0; }
        }
        @keyframes fadeInUp {
          from { opacity: 0; transform: translateY(20px); }
          to { opacity: 1; transform: translateY(0); }
        }
        .scan-line {
          position: absolute;
          left: 0; top: -100%;
          width: 100%; height: 3px;
          background: linear-gradient(to bottom, transparent, #00f5d4, transparent);
          animation: scanBoot 1.8s ease-out 0.3s 1 forwards;
          opacity: 0;
          pointer-events: none;
          z-index: 99;
        }
        .scanline-overlay {
          position: absolute;
          left: 0; top: 0;
          width: 100%; height: 100%;
          background: repeating-linear-gradient(
            0deg,
            transparent,
            transparent 3px,
            rgba(0,245,212,0.015) 3px,
            rgba(0,245,212,0.015) 4px
          );
          pointer-events: none;
          border-radius: 16px;
        }
        .scan-line-h {
          position: absolute;
          left: 0; top: 0;
          width: 100%; height: 2px;
          background: linear-gradient(to right, transparent, #00f5d4 30%, #00f5d4 70%, transparent);
          opacity: 0;
          animation: scanBoot 2s ease-out 0.2s 1 forwards;
        }
        .container {
          position: relative;
          width: 900px;
          height: 480px;
          overflow: hidden;
          border-radius: 16px;
          font-family: 'Courier New', 'Consolas', monospace;
        }
      </style>
      <div class="container">
        <div class="scanline-overlay"></div>
        <div class="scan-line"></div>
      </div>
    </div>
  </foreignObject>

  <!-- ═══════════════ LEFT SIDE — MAIN HERO CONTENT ═══════════════ -->
  <foreignObject x="28" y="0" width="530" height="480">
    <div xmlns="http://www.w3.org/1999/xhtml">
      <style>
        * { box-sizing: border-box; margin: 0; padding: 0; }

        /* ── GLITCH ANIMATION ── */
        @keyframes glitch1 {
          0%, 85%, 100% { clip-path: none; transform: none; opacity:1; }
          86% { clip-path: polygon(0 15%, 100% 15%, 100% 30%, 0 30%); transform: translateX(-4px); opacity: 0.9; }
          87% { clip-path: polygon(0 50%, 100% 50%, 100% 65%, 0 65%); transform: translateX(4px); opacity: 0.8; }
          88% { clip-path: polygon(0 70%, 100% 70%, 100% 85%, 0 85%); transform: translateX(-2px); opacity: 0.9; }
          89% { clip-path: none; transform: none; opacity: 1; }
        }
        @keyframes glitch2 {
          0%, 85%, 100% { opacity: 0; transform: none; }
          86% { clip-path: polygon(0 0%, 100% 0%, 100% 25%, 0 25%); transform: translateX(6px); opacity: 0.5; color: #a855f7; }
          87% { clip-path: polygon(0 40%, 100% 40%, 100% 60%, 0 60%); transform: translateX(-6px); opacity: 0.4; color: #00f5d4; }
          88% { opacity: 0; }
        }
        @keyframes glitch3 {
          0%, 85%, 100% { opacity: 0; }
          86% { clip-path: polygon(0 60%, 100% 60%, 100% 80%, 0 80%); transform: translateX(-3px); opacity: 0.35; color: #f59e0b; }
          87.5% { opacity: 0; }
        }

        /* ── TYPEWRITER ── */
        @keyframes typewriter {
          0% { width: 0; }
          40% { width: 100%; }
          60% { width: 100%; }
          65% { width: 0; }
          100% { width: 0; }
        }
        @keyframes typewriter2 {
          0%, 42% { width: 0; opacity: 0; }
          43% { opacity: 1; }
          60% { width: 100%; opacity: 1; }
          75% { width: 100%; opacity: 1; }
          80% { width: 0; opacity: 0; }
          100% { width: 0; opacity: 0; }
        }
        @keyframes typewriter3 {
          0%, 62% { width: 0; opacity: 0; }
          63% { opacity: 1; }
          80% { width: 100%; opacity: 1; }
          95% { width: 100%; opacity: 1; }
          100% { width: 100%; opacity: 1; }
        }
        @keyframes blink {
          0%, 100% { opacity: 1; }
          50% { opacity: 0; }
        }

        /* ── SYSTEM LABEL ── */
        @keyframes labelFlicker {
          0%, 90%, 100% { opacity: 1; }
          92% { opacity: 0.3; }
          94% { opacity: 1; }
          96% { opacity: 0.5; }
          98% { opacity: 1; }
        }

        /* ── TAG PILLS ── */
        @keyframes tagPulse1 {
          0%, 100% { box-shadow: 0 0 6px #00f5d4, 0 0 0 1px #00f5d4; }
          50% { box-shadow: 0 0 14px #00f5d4, 0 0 0 1px #00f5d4, 0 0 20px rgba(0,245,212,0.3); }
        }
        @keyframes tagPulse2 {
          0%, 100% { box-shadow: 0 0 6px #a855f7, 0 0 0 1px #a855f7; }
          50% { box-shadow: 0 0 14px #a855f7, 0 0 0 1px #a855f7, 0 0 20px rgba(168,85,247,0.3); }
        }
        @keyframes tagPulse3 {
          0%, 100% { box-shadow: 0 0 6px #f59e0b, 0 0 0 1px #f59e0b; }
          50% { box-shadow: 0 0 14px #f59e0b, 0 0 0 1px #f59e0b, 0 0 20px rgba(245,158,11,0.3); }
        }

        /* ── FADE IN STAGGER ── */
        @keyframes fadeUp { from { opacity:0; transform:translateY(16px); } to { opacity:1; transform:translateY(0); } }
        @keyframes fadeIn { from { opacity:0; } to { opacity:1; } }

        /* ── STATUS DOT ── */
        @keyframes statusPulse {
          0%, 100% { box-shadow: 0 0 0 0 rgba(0,245,212,0.7); transform: scale(1); }
          50% { box-shadow: 0 0 0 6px rgba(0,245,212,0); transform: scale(1.2); }
        }

        .hero-left {
          padding: 28px 0 28px 0;
          height: 480px;
          display: flex;
          flex-direction: column;
          justify-content: center;
          gap: 0;
        }

        .system-label {
          font-family: 'Courier New', monospace;
          font-size: 11px;
          letter-spacing: 3px;
          color: #00f5d4;
          text-transform: uppercase;
          margin-bottom: 10px;
          animation: labelFlicker 8s infinite 2s, fadeUp 0.6s ease-out 0.3s both;
          display: flex;
          align-items: center;
          gap: 8px;
        }
        .status-dot {
          width: 7px;
          height: 7px;
          background: #00f5d4;
          border-radius: 50%;
          display: inline-block;
          animation: statusPulse 2s ease-in-out infinite;
        }

        .name-wrapper {
          position: relative;
          animation: fadeUp 0.7s ease-out 0.5s both;
        }
        .name-main {
          font-family: 'Courier New', Consolas, monospace;
          font-size: 52px;
          font-weight: 900;
          color: #e8f4ff;
          line-height: 1.05;
          letter-spacing: -1px;
          text-transform: uppercase;
          animation: glitch1 7s infinite 3s;
          position: relative;
          display: block;
          text-shadow: 0 0 30px rgba(0,245,212,0.2);
        }
        .name-glitch-a {
          position: absolute;
          top: 0; left: 0;
          font-family: 'Courier New', Consolas, monospace;
          font-size: 52px;
          font-weight: 900;
          color: #00f5d4;
          line-height: 1.05;
          letter-spacing: -1px;
          text-transform: uppercase;
          animation: glitch2 7s infinite 3s;
          pointer-events: none;
          user-select: none;
        }
        .name-glitch-b {
          position: absolute;
          top: 0; left: 0;
          font-family: 'Courier New', Consolas, monospace;
          font-size: 52px;
          font-weight: 900;
          color: #a855f7;
          line-height: 1.05;
          letter-spacing: -1px;
          text-transform: uppercase;
          animation: glitch3 7s infinite 3s;
          pointer-events: none;
          user-select: none;
        }

        .tagline-row {
          margin-top: 12px;
          animation: fadeUp 0.7s ease-out 0.8s both;
        }
        .tagline-static {
          font-family: 'Courier New', monospace;
          font-size: 22px;
          font-weight: 700;
          color: #00f5d4;
          letter-spacing: 1px;
          text-shadow: 0 0 12px rgba(0,245,212,0.6);
        }

        .typewriter-row {
          margin-top: 8px;
          height: 22px;
          animation: fadeUp 0.7s ease-out 1s both;
        }
        .tw-wrapper {
          font-family: 'Courier New', monospace;
          font-size: 13px;
          color: #94a3b8;
          letter-spacing: 1px;
          display: flex;
          align-items: center;
          gap: 2px;
          height: 22px;
          position: relative;
          overflow: hidden;
        }
        .tw-text-1 {
          white-space: nowrap;
          overflow: hidden;
          display: inline-block;
          animation: typewriter 12s infinite 1.5s;
          position: absolute;
        }
        .tw-text-2 {
          white-space: nowrap;
          overflow: hidden;
          display: inline-block;
          animation: typewriter2 12s infinite 1.5s;
          position: absolute;
        }
        .tw-text-3 {
          white-space: nowrap;
          overflow: hidden;
          display: inline-block;
          animation: typewriter3 12s infinite 1.5s;
          position: absolute;
        }
        .tw-cursor {
          animation: blink 0.8s step-end infinite;
          color: #00f5d4;
          font-weight: bold;
          position: relative;
          z-index: 2;
          margin-left: 1px;
        }

        .divider {
          width: 60px;
          height: 2px;
          background: linear-gradient(to right, #00f5d4, transparent);
          margin: 16px 0;
          animation: fadeIn 0.5s ease-out 1.2s both;
        }

        .tags-row {
          display: flex;
          gap: 8px;
          flex-wrap: wrap;
          animation: fadeUp 0.7s ease-out 1.3s both;
          margin-bottom: 20px;
        }
        .tag {
          font-family: 'Courier New', monospace;
          font-size: 10px;
          font-weight: 700;
          letter-spacing: 2px;
          text-transform: uppercase;
          padding: 6px 14px;
          border-radius: 4px;
          border: none;
        }
        .tag-1 {
          color: #00f5d4;
          background: rgba(0,245,212,0.08);
          animation: tagPulse1 3s ease-in-out infinite 0s;
        }
        .tag-2 {
          color: #a855f7;
          background: rgba(168,85,247,0.08);
          animation: tagPulse2 3s ease-in-out infinite 0.8s;
        }
        .tag-3 {
          color: #f59e0b;
          background: rgba(245,158,11,0.08);
          animation: tagPulse3 3s ease-in-out infinite 1.6s;
        }

        .badges-row {
          display: flex;
          gap: 8px;
          flex-wrap: wrap;
          animation: fadeUp 0.7s ease-out 1.6s both;
        }
        .badge {
          font-family: 'Courier New', monospace;
          font-size: 9px;
          font-weight: 600;
          letter-spacing: 1px;
          padding: 4px 10px;
          border-radius: 3px;
          text-decoration: none;
          border: 1px solid;
          text-transform: uppercase;
        }
        .badge-teal { color: #00f5d4; border-color: rgba(0,245,212,0.35); background: rgba(0,245,212,0.05); }
        .badge-purple { color: #a855f7; border-color: rgba(168,85,247,0.35); background: rgba(168,85,247,0.05); }
        .badge-amber { color: #f59e0b; border-color: rgba(245,158,11,0.35); background: rgba(245,158,11,0.05); }
        .badge-green { color: #4ade80; border-color: rgba(74,222,128,0.35); background: rgba(74,222,128,0.05); }
      </style>
      <div class="hero-left">
        <div class="system-label">
          <span class="status-dot"></span>
          SYSTEM://SIGNAL_MESH &nbsp;·&nbsp; NEURAL_COMMAND_DECK v2.0
        </div>

        <div class="name-wrapper">
          <span class="name-main">MUSHARAF<br/>KHAN PATHAN</span>
          <span class="name-glitch-a" aria-hidden="true">MUSHARAF<br/>KHAN PATHAN</span>
          <span class="name-glitch-b" aria-hidden="true">MUSHARAF<br/>KHAN PATHAN</span>
        </div>

        <div class="tagline-row">
          <span class="tagline-static">Build. Secure. Scale.</span>
        </div>

        <div class="typewriter-row">
          <div class="tw-wrapper">
            <span class="tw-text-1">Software Engineer &nbsp;|&nbsp; AI × Security &nbsp;|&nbsp; Backend Systems</span>
            <span class="tw-text-2">Spring Boot · Python · Docker · AWS · React</span>
            <span class="tw-text-3">Chicago, IL &nbsp;·&nbsp; IIT BS CS May 2026 &nbsp;·&nbsp; Open to FT Roles</span>
            <span class="tw-cursor">█</span>
          </div>
        </div>

        <div class="divider"></div>

        <div class="tags-row">
          <span class="tag tag-1">Low Latency</span>
          <span class="tag tag-2">Secure by Design</span>
          <span class="tag tag-3">Ship Fast</span>
        </div>

        <div class="badges-row">
          <span class="badge badge-teal">📁 Portfolio</span>
          <span class="badge badge-purple">🔗 LinkedIn</span>
          <span class="badge badge-amber">✉ Direct Contact</span>
          <span class="badge badge-green">⚡ FT SWE 2026</span>
        </div>
      </div>
    </div>
  </foreignObject>

  <!-- ═══════════════ DIVIDER LINE ═══════════════ -->
  <line x1="555" y1="40" x2="555" y2="440" stroke="#00f5d4" stroke-width="0.5" stroke-opacity="0.25"/>

  <!-- ═══════════════ RIGHT SIDE — RADAR + SIGNAL BOARD ═══════════════ -->

  <!-- Signal Board header -->
  <foreignObject x="570" y="28" width="300" height="30">
    <div xmlns="http://www.w3.org/1999/xhtml">
      <style>
        @keyframes headerGlow {
          0%,100% { text-shadow: 0 0 8px rgba(0,245,212,0.5); }
          50% { text-shadow: 0 0 16px rgba(0,245,212,0.9), 0 0 30px rgba(0,245,212,0.4); }
        }
        .board-header {
          font-family: 'Courier New', monospace;
          font-size: 11px;
          font-weight: 700;
          color: #00f5d4;
          letter-spacing: 3px;
          text-transform: uppercase;
          animation: headerGlow 3s ease-in-out infinite;
        }
        .board-sub {
          font-family: 'Courier New', monospace;
          font-size: 9px;
          color: rgba(148,163,184,0.7);
          letter-spacing: 1px;
          margin-top: 2px;
        }
      </style>
      <div class="board-header">SIGNAL_BOARD://LIVE</div>
      <div class="board-sub">EXECUTION + SECURITY + DELIVERY PROFILE</div>
    </div>
  </foreignObject>

  <!-- ═══ RADAR CIRCLE ═══ -->
  <!-- Radar background panel -->
  <rect x="575" y="68" width="300" height="220" rx="10" fill="rgba(10,30,60,0.7)" stroke="#00f5d4" stroke-width="0.5" stroke-opacity="0.3"/>

  <!-- Radar grid circles -->
  <g transform="translate(725, 178)">
    <!-- Concentric rings -->
    <circle r="80" fill="none" stroke="#00f5d4" stroke-width="0.5" stroke-opacity="0.2"/>
    <circle r="60" fill="none" stroke="#00f5d4" stroke-width="0.5" stroke-opacity="0.25"/>
    <circle r="40" fill="none" stroke="#00f5d4" stroke-width="0.5" stroke-opacity="0.3"/>
    <circle r="20" fill="none" stroke="#00f5d4" stroke-width="0.5" stroke-opacity="0.4"/>

    <!-- Cross-hairs -->
    <line x1="-82" y1="0" x2="82" y2="0" stroke="#00f5d4" stroke-width="0.5" stroke-opacity="0.15"/>
    <line x1="0" y1="-82" x2="0" y2="82" stroke="#00f5d4" stroke-width="0.5" stroke-opacity="0.15"/>
    <line x1="-58" y1="-58" x2="58" y2="58" stroke="#00f5d4" stroke-width="0.3" stroke-opacity="0.1"/>
    <line x1="58" y1="-58" x2="-58" y2="58" stroke="#00f5d4" stroke-width="0.3" stroke-opacity="0.1"/>

    <!-- Radar sweep (rotating sector) -->
    <g>
      <animateTransform attributeName="transform" type="rotate" values="0;360" dur="4s" repeatCount="indefinite"/>
      <!-- Sweep sector path: pie slice from 0 to ~60 degrees -->
      <path d="M 0 0 L 82 0 A 82 82 0 0 0 41 -71 Z" fill="url(#sweepGrad)" opacity="0.6">
        <animate attributeName="opacity" values="0.4;0.8;0.4" dur="4s" repeatCount="indefinite"/>
      </path>
      <!-- Sweep leading edge line -->
      <line x1="0" y1="0" x2="82" y2="0" stroke="#00f5d4" stroke-width="1.5" stroke-opacity="0.8"/>
    </g>

    <!-- Center dot -->
    <circle r="4" fill="#00f5d4" filter="url(#tealGlowStrong)"/>
    <circle r="2" fill="#ffffff"/>

    <!-- Signal dot 1 — pulsing at position ~45deg, r=55 -->
    <g transform="translate(39, -39)">
      <circle r="5" fill="#00f5d4" opacity="0.9" filter="url(#tealGlow)">
        <animate attributeName="r" values="4;7;4" dur="2.3s" repeatCount="indefinite"/>
        <animate attributeName="opacity" values="0.9;0.3;0.9" dur="2.3s" repeatCount="indefinite"/>
      </circle>
      <circle r="2" fill="#00f5d4"/>
    </g>

    <!-- Signal dot 2 — pulsing at position ~200deg, r=35 -->
    <g transform="translate(-33, 12)">
      <circle r="5" fill="#a855f7" opacity="0.8" filter="url(#purpleGlow)">
        <animate attributeName="r" values="3;6;3" dur="3.1s" repeatCount="indefinite" begin="0.7s"/>
        <animate attributeName="opacity" values="0.8;0.2;0.8" dur="3.1s" repeatCount="indefinite" begin="0.7s"/>
      </circle>
      <circle r="2" fill="#a855f7"/>
    </g>

    <!-- Signal dot 3 — pulsing at position ~310deg, r=65 -->
    <g transform="translate(22, 62)">
      <circle r="5" fill="#f59e0b" opacity="0.7" filter="url(#tealGlow)">
        <animate attributeName="r" values="3;5;3" dur="1.9s" repeatCount="indefinite" begin="1.4s"/>
        <animate attributeName="opacity" values="0.7;0.2;0.7" dur="1.9s" repeatCount="indefinite" begin="1.4s"/>
      </circle>
      <circle r="2" fill="#f59e0b"/>
    </g>

    <!-- Outer ping ring -->
    <circle r="80" fill="none" stroke="#00f5d4" stroke-width="1" stroke-opacity="0">
      <animate attributeName="r" values="0;90" dur="3s" repeatCount="indefinite" begin="0s"/>
      <animate attributeName="stroke-opacity" values="0.5;0" dur="3s" repeatCount="indefinite" begin="0s"/>
    </circle>
  </g>

  <!-- RADAR label -->
  <text x="587" y="84" font-family="'Courier New', monospace" font-size="9" fill="#00f5d4" opacity="0.6" letter-spacing="2">SIGNAL://RADAR_LIVE</text>

  <!-- ═══ SIGNAL PROOF BARS ═══ -->
  <foreignObject x="575" y="305" width="300" height="165">
    <div xmlns="http://www.w3.org/1999/xhtml">
      <style>
        @keyframes barFill {
          from { width: 0%; }
        }
        @keyframes barShimmer {
          0% { left: -100%; }
          100% { left: 200%; }
        }
        @keyframes heartbeat {
          0%, 100% { box-shadow: 0 0 4px #4ade80; }
          30% { box-shadow: 0 0 12px #4ade80, 0 0 20px rgba(74,222,128,0.4); }
          60% { box-shadow: 0 0 4px #4ade80; }
          80% { box-shadow: 0 0 10px #4ade80, 0 0 18px rgba(74,222,128,0.3); }
        }
        @keyframes barFade { from { opacity: 0; transform: translateX(-10px); } to { opacity: 1; transform: translateX(0); } }

        .proof-panel {
          font-family: 'Courier New', monospace;
          padding: 4px 0;
        }
        .proof-header {
          display: flex;
          align-items: center;
          gap: 8px;
          margin-bottom: 14px;
        }
        .proof-title {
          font-size: 11px;
          font-weight: 700;
          color: #00f5d4;
          letter-spacing: 2px;
          text-transform: uppercase;
        }
        .proof-dot {
          width: 8px; height: 8px;
          border-radius: 50%;
          background: #4ade80;
          animation: heartbeat 1.5s ease-in-out infinite;
        }
        .bar-row {
          margin-bottom: 11px;
          animation: barFade 0.5s ease-out both;
        }
        .bar-row:nth-child(2) { animation-delay: 0.2s; }
        .bar-row:nth-child(3) { animation-delay: 0.4s; }
        .bar-row:nth-child(4) { animation-delay: 0.6s; }
        .bar-row:nth-child(5) { animation-delay: 0.8s; }
        .bar-label-row {
          display: flex;
          justify-content: space-between;
          align-items: center;
          margin-bottom: 4px;
        }
        .bar-label {
          font-size: 11px;
          font-weight: 700;
          color: #e2e8f0;
          letter-spacing: 0.5px;
        }
        .bar-pct {
          font-size: 10px;
          font-weight: 700;
          letter-spacing: 1px;
        }
        .bar-track {
          width: 100%;
          height: 6px;
          background: rgba(255,255,255,0.07);
          border-radius: 3px;
          overflow: hidden;
          position: relative;
        }
        .bar-fill {
          height: 100%;
          border-radius: 3px;
          position: relative;
          overflow: hidden;
          animation: barFill 1.4s cubic-bezier(0.4,0,0.2,1) both;
        }
        .bar-fill::after {
          content: '';
          position: absolute;
          top: 0; left: -100%;
          width: 60%;
          height: 100%;
          background: linear-gradient(to right, transparent, rgba(255,255,255,0.4), transparent);
          animation: barShimmer 2.5s ease-in-out infinite 1.5s;
        }
        .bar-exec { width: 87%; background: linear-gradient(to right, #00f5d4, #00c9a7); box-shadow: 0 0 8px rgba(0,245,212,0.5); animation-delay: 0.5s; }
        .bar-sec { width: 92%; background: linear-gradient(to right, #a855f7, #7c3aed); box-shadow: 0 0 8px rgba(168,85,247,0.5); animation-delay: 0.8s; }
        .bar-del { width: 88%; background: linear-gradient(to right, #f59e0b, #d97706); box-shadow: 0 0 8px rgba(245,158,11,0.5); animation-delay: 1.1s; }
        .bar-scale { width: 93%; background: linear-gradient(to right, #4ade80, #16a34a); box-shadow: 0 0 8px rgba(74,222,128,0.5); animation-delay: 1.4s; }
        .pct-teal { color: #00f5d4; }
        .pct-purple { color: #a855f7; }
        .pct-amber { color: #f59e0b; }
        .pct-green { color: #4ade80; }
        .hiring-badge {
          display: inline-block;
          margin-top: 6px;
          font-size: 9px;
          font-weight: 700;
          letter-spacing: 2px;
          color: #4ade80;
          background: rgba(74,222,128,0.08);
          border: 1px solid rgba(74,222,128,0.4);
          padding: 4px 10px;
          border-radius: 3px;
          text-transform: uppercase;
          animation: heartbeat 1.5s ease-in-out infinite;
        }
      </style>
      <div class="proof-panel">
        <div class="proof-header">
          <span class="proof-title">SIGNAL://PROOF</span>
          <span class="proof-dot"></span>
        </div>

        <div class="bar-row">
          <div class="bar-label-row">
            <span class="bar-label">Execution</span>
            <span class="bar-pct pct-teal">87%</span>
          </div>
          <div class="bar-track"><div class="bar-fill bar-exec"></div></div>
        </div>

        <div class="bar-row">
          <div class="bar-label-row">
            <span class="bar-label">Security Mindset</span>
            <span class="bar-pct pct-purple">92%</span>
          </div>
          <div class="bar-track"><div class="bar-fill bar-sec"></div></div>
        </div>

        <div class="bar-row">
          <div class="bar-label-row">
            <span class="bar-label">AI Product Delivery</span>
            <span class="bar-pct pct-amber">88%</span>
          </div>
          <div class="bar-track"><div class="bar-fill bar-del"></div></div>
        </div>

        <div class="bar-row">
          <div class="bar-label-row">
            <span class="bar-label">Scale + Reliability</span>
            <span class="bar-pct pct-green">93%</span>
          </div>
          <div class="bar-track"><div class="bar-fill bar-scale"></div></div>
        </div>

        <span class="hiring-badge">⚡ HIRING_READINESS://HIGH</span>
      </div>
    </div>
  </foreignObject>

  <!-- Decorative corner element top-right -->
  <text x="876" y="20" font-family="'Courier New', monospace" font-size="8" fill="#00f5d4" opacity="0.35" text-anchor="end" letter-spacing="1">v2.0.1</text>
  <text x="876" y="467" font-family="'Courier New', monospace" font-size="8" fill="#00f5d4" opacity="0.3" text-anchor="end" letter-spacing="1">SIGNAL_ACTIVE</text>

  <!-- Bottom status bar -->
  <rect x="1" y="458" width="898" height="21" rx="0" fill="rgba(0,245,212,0.04)" clip-path="url(#mainClip)"/>
  <line x1="1" y1="458" x2="899" y2="458" stroke="#00f5d4" stroke-width="0.5" stroke-opacity="0.2"/>
  <text x="18" y="472" font-family="'Courier New', monospace" font-size="9" fill="#00f5d4" opacity="0.45" letter-spacing="1.5">SYS://ONLINE</text>
  <circle cx="93" cy="468" r="3" fill="#4ade80" opacity="0.7">
    <animate attributeName="opacity" values="0.7;0.2;0.7" dur="1.5s" repeatCount="indefinite"/>
  </circle>
  <text x="455" y="472" font-family="'Courier New', monospace" font-size="9" fill="#94a3b8" opacity="0.4" text-anchor="middle" letter-spacing="1">MUSHARAF KHAN PATHAN · CHICAGO IL · IIT CS MAY 2026</text>
</svg>
